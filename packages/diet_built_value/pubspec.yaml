name: diet_built_value
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  built_value: ^7.0.0
  built_collection: ^4.0.0
dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  built_value_generator:
  build_runner: ^1.0.0
